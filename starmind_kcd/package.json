{
  "name": "starmind_kcd",
  "version": "0.1.0",
  "description": "KCD MCP server — thin I/O gate over kcd_sdk",
  "scripts": {
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit && node build.js",
    "dev": "tsx watch src/index.ts",
    "verify": "tsx src/scripts/verify.ts",
    "snapshot": "tsx src/scripts/snapshot.ts"
  },
  "dependencies": {
    "kcd_sdk": "file:../../kcd_sdk"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "tsx": "^4.0.0",
    "@types/node": "^20.0.0"
  }
}
