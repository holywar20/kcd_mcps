{
  "name": "starmind_semantic_browser",
  "version": "0.1.0",
  "description": "Starmind semantic-browser MCP server — read web pages distilled, act on elements by ref, over CDP behind an origin whitelist",
  "scripts": {
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit && node build.js",
    "dev": "tsx watch src/index.ts"
  },
  "dependencies": {
    "kcd_sdk": "file:../../kcd_sdk"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "tsx": "^4.0.0",
    "@types/node": "^22.0.0"
  }
}
